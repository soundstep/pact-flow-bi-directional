{
  "name": "consumer-spa",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.3.0",
    "@testing-library/user-event": "^13.5.0",
    "axios": "^0.27.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "serve": "serve -l 8080 ./build",
    "cypress:open": "cypress open",
    "cypress:up": "pnpm serve & wait-on http://localhost:8080",
    "cypress:run": "cypress run",
    "cypress:down": "kill-port --port 8080"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@pact-foundation/pact-web": "^9.18.1",
    "@pactflow/pact-cypress-adapter": "^1.2.6",
    "cypress": "^10.3.1",
    "kill-port": "^2.0.1",
    "serve": "^14.0.1",
    "wait-on": "^6.0.1"
  }
}
